export enum PublicRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
  LANDING = "/landing",
}

export enum PrivateRoutes {
  PRIVATE = "/private",
  DASHBOARD = "/dashboard",
  HOME = "/home",
}

export const baseUrl = "http://localhost:3000";